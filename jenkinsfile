pipeline {
    agent any

    stages {
        stage('Clone repository') {
            steps {
                git branch: 'main', url: 'https://github.com/Sonalipsa1234/basic-calculator'
            }
        }

        stage('Build image') {
            steps {
                bat """
                docker build -t sonalipsa/calculator-app .
                """
            }
        }

        stage('Test image') {
            steps {
                bat """
                docker run --rm sonalipsa/calculator-app sh -c "npm test || echo 'No tests found, skipping...'"
                """
            }
        }

        stage('Push image') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'dockerhub-credentials', passwordVariable: 'DOCKER_PASSWORD', usernameVariable: 'DOCKER_USERNAME')]) {
                    bat """
                    echo %DOCKER_PASSWORD% | docker login -u %DOCKER_USERNAME% --password-stdin
                    docker tag sonalipsa/calculator-app %DOCKER_USERNAME%/calculator-app:latest
                    docker push %DOCKER_USERNAME%/calculator-app:latest
                    """
                }
            }
        }
    }
}
